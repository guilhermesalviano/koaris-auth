# sqlite
# DATABASE_URL="file:./dev.db"

# run postgres with docker only
# docker start/stop postgres-local
# npx prisma migrate reset/deploy

# UP a new database: 
# 1st docker run -d --name postgres-local -e POSTGRES_USER="username" -e POSTGRES_PASSWORD="password" -e POSTGRES_DB="koaris-auth" -p 5432:5432 -v postgres-data:/var/lib/postgresql/data bitnami/postgresql:latest
# 2nd npx prisma generate
# 3rd npx prisma migrate deploy

POSTGRES_USER=username
POSTGRES_PASSWORD=password
POSTGRES_DB=koaris-auth
DATABASE_URL=postgresql://username:password@127.0.0.1:5432/koaris-auth?schema=public